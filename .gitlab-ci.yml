image: node

stages:
  - install
  - build
  - test

cache:
  paths:
    - node_modules/

install dependences:
  stage: install
  script:
    - npm install

build the project:
  stage: build
  script:
    - npm run generate
  artifacts:
    paths:
      - dist
  only:
    - master

test static:
  image: alpine
  stage: test
  script:
    - grep -q "Michigan" ./dist/index.html

test universal:
  image: node
  stage: test
  script:
    - npm run build
    - npm run start
    - curl "http://localhost:3000" | grep -q "Michigan" ./dist/index.html
